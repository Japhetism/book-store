import { connect } from 'react-redux';
import PersonalAccountScreen from './screen';

function mapStateToProps() {
    return {

    }
}

function mapDispatchToProps(dispatch) {
    return {

    }
}

export default connect(mapStateToProps, mapDispatchToProps)(PersonalAccountScreen);